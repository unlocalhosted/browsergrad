# @unlocalhosted/browsergrad-primitives

All notable changes to `@unlocalhosted/browsergrad-primitives`.

## 0.1.1

- Reissued the unchanged runtime surface through the immutable pnpm-packed
  release pipeline so staged bytes and registry bytes have one reproducible
  package-manifest contract.

## 0.1.0

- Added primitive-first facade package for text, data, evaluation, scaling,
  simulation, and RL helpers.
