{
  "name": "browsergrad",
  "private": true,
  "version": "0.0.0",
  "description": "Workspace root for the @unlocalhosted/browsergrad-* library family. Not published — install individual packages from npm.",
  "type": "module",
  "packageManager": "pnpm@10.34.5",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm run architecture:check && pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "lint": "pnpm -r run lint",
    "architecture:check": "node scripts/semantic-architecture-check.mjs",
    "architecture:generate-requirements": "node scripts/generate-assignment-requirement-usage.mjs",
    "verify:release": "node scripts/verify-release.mjs",
    "test:release-packages": "python3 scripts/snapshot-package-tar.test.py && node --test scripts/npm-read-only-environment.test.mjs scripts/package-publish-order.test.mjs scripts/publish-missing-npm.test.mjs scripts/require-npm-version.test.mjs scripts/verify-published-package-equivalence.test.mjs scripts/verify-npm-provenance.test.mjs && node scripts/test-release-packages.mjs",
    "validate:prd": "node scripts/validate-research-gated-prd.mjs",
    "clean": "pnpm -r run clean && rm -rf node_modules"
  },
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/unlocalhosted/browsergrad.git"
  },
  "license": "MIT",
  "author": "unlocalhosted"
}
